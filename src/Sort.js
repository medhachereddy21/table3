import React, { Component } from "react";
import { Form } from "semantic-ui-react";

class Sort extends Component {
  render() {
    return <Button onClick="sortData"> {item} </Button>;
  }
}

export default Search;
